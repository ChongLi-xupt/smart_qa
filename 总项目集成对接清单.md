# 智慧问数模块 × 总项目集成对接清单

> 本文档面向**总项目负责人**：智慧问数（NL2SQL）作为总项目的一个子模块，
> 列出集成上线需要总项目侧提供/确认的全部事项，以及模块侧的适配承诺。
> 资源类需求（数据库、模型、服务器明细）见《部署需求清单.md》，本文不重复展开。

---

## 〇、模块自述（供负责人快速了解）

| 项 | 现状 |
|---|---|
| 形态 | 独立 FastAPI 微服务，环境变量配置，无硬编码依赖，可容器化 |
| 核心接口 | `POST /api/v1/ask`（同步）、`POST /api/v1/ask_stream`（SSE 流式） |
| 配套接口 | 页面 `GET /`、会话管理 `/api/v1/sessions*`、反馈 `/api/v1/feedback`、健康检查 `GET /healthz` |
| 接口文档 | 随附 `API.md`（含请求/响应结构、错误码、SSE 事件格式） |
| 用户隔离 | 已支持 `user_id` 会话归属与按用户隔离展示（当前为客户端自报，**待升级为可信身份**，见 §一-2） |
| 安全基线 | 数据库只读 + SQL 护栏、敏感问题拦截、错误脱敏、可选 `X-API-Key`、签名会话 cookie |
| 运行依赖 | Python ≥3.10；MySQL（只读）；OpenAI 兼容 LLM；Redis（可选） |

---

## 一、需要总项目负责人提供 / 确认

### 1. 集成形态（三选一，决定后续所有对接项）

| 方案 | 说明 | 需总项目提供 |
|---|---|---|
| **A. 独立微服务 + 统一网关转发（推荐）** | 模块独立部署，总网关路由转发 | 网关路由规则、前缀映射 |
| B. 页面嵌入 | 模块页面以 iframe/微前端嵌入总前端 | 嵌入方式、域名规划、跨域策略 |
| C. 纯后端 API | 总前端自研 UI，只调本模块 API | 前端调用规范、CORS 域名清单 |

### 2. 用户身份与认证边界（**最重要**）

模块当前 `user_id` 由客户端自报，仅适合演示；接入总项目必须改用**可信身份**。请提供：

| 提供项 | 说明 |
|---|---|
| 用户 ID 规范 | 格式、长度、字符集（模块侧校验上限当前 64 字符，可调整） |
| 身份注入方式 | 网关认证后注入的请求头名称（如 `X-User-Id`），或 JWT 由谁解析 |
| 认证边界 | 登录态归总网关/SSO 还是模块自带 `X-API-Key`；二者只留一层 |
| 会话 cookie 去留 | 模块自带签名 cookie（`chat_session_id`）与总项目登录态是否冲突，是否保留 |

交付物建议：《统一认证与用户身份注入规范》一页纸即可。

### 3. 统一资源（若总项目集中供给）

| 提供项 | 说明 |
|---|---|
| 数据库 | 只读账号 + 网络白名单（或 DBA 对接人，授权 SQL 见《部署需求清单》） |
| 大模型 | 统一模型网关地址 / Key 申请流程 / 配额与限流说明（单问 10~60s，需支持长连接） |
| 运行环境 | 服务器或容器平台（K8s 命名空间）、镜像仓库地址、CI/CD 流水线 |
| 配置注入方式 | 环境变量 / 配置中心 / Secret 管理方案（模块全部配置走环境变量，天然兼容） |
| Redis | 多实例部署时的共享会话存储地址 |

### 4. 接口与规范对齐

| 对齐项 | 模块现状 | 需总项目提供 |
|---|---|---|
| 路由前缀 | `/api/v1/*` | 网关映射规则（如 `/nl2sql/api/v1/*`）及是否改写路径 |
| 响应结构 | 业务字段直展 + `{"ok","error"}` 错误体 | 若需统一包装（如 `{code,msg,data}`），提供规范，模块加适配层 |
| 错误码 | HTTP 状态码 + 文案（见 API.md §1.5） | 总项目错误码对照表 |
| 链路追踪 | 自带 `X-Request-ID`（8 位 hex）响应头与日志 | 统一 trace 头名称（如 `X-Trace-Id`），模块可改名对齐 |
| 跨域 CORS | 当前未启用 | 前端域名清单（方案 B/C 必需），或网关同源转发免 CORS |
| 限流分工 | 模块按 IP 限流（默认 20 次/分） | 网关层限流策略，避免双层叠加误伤 |

### 5. 网关 / 接入层配置要求（请负责人转运维落实）

| 配置 | 原因 |
|---|---|
| `/ask_stream` 关闭缓冲 | SSE 实时推送；nginx 需 `proxy_buffering off`（模块已下发 `X-Accel-Buffering: no`，需网关尊重） |
| 超时 ≥ 120s | 单问涉及多轮 LLM 工具调用，典型 10~60s，默认 60s 网关超时会掐断 |
| 透传 `Set-Cookie` | 会话 cookie 机制依赖，网关不得剥离 |
| 保留客户端真实 IP | 模块限流/日志依赖 `X-Forwarded-For` |

### 6. 数据与合规

| 提供项 | 说明 |
|---|---|
| 接入库表确认 | 明确问答覆盖哪些库/表，由负责人协调业务方 |
| 术语/语料对接人 | 业务术语表、高频问答 few-shot 语料的维护责任人 |
| 合规要求 | 敏感数据范围、审计留痕、数据出域限制（模型走公网时需确认） |

### 7. 流程与组织

| 提供项 | 说明 |
|---|---|
| 代码仓库 | 地址、分支策略、评审流程 |
| 发布流程 | 发布窗口、变更审批、回滚方案 |
| 验收标准与验收人 | 以《部署需求清单》§六 为基础，负责人确认或补充 |
| 对接人清单 | 网关/运维/DBA/模型平台/前端 各一名接口人 |

---

## 二、模块侧适配承诺（拿到上述规范后由模块完成）

1. `user_id` 改为读取网关注入的可信身份头，废弃客户端自报（保留自报为降级兜底）；
2. 按需增加统一响应包装适配层、trace 头改名、CORS 白名单配置；
3. 提供 Dockerfile / 健康检查 / 日志标准输出，适配容器平台与日志采集；
4. 配合联调：SSE 流式在网关后的实时性验证、多用户隔离验证、401/403 链路验证。

---

## 三、对接确认表（逐项勾选）

| # | 事项 | 责任方 | 状态 |
|---|---|---|---|
| 1 | 集成形态定案（A/B/C） | 总负责人 | ☐ |
| 2 | 用户身份注入规范（头名称/ID 格式） | 总负责人 | ☐ |
| 3 | 认证边界与 cookie 去留结论 | 总负责人 | ☐ |
| 4 | DB 只读账号与白名单 | DBA | ☐ |
| 5 | 模型网关地址/Key/配额 | 模型平台 | ☐ |
| 6 | 部署环境（机器或容器平台+CI） | 运维 | ☐ |
| 7 | 网关路由/超时/SSE 缓冲配置 | 运维 | ☐ |
| 8 | 响应结构/错误码/trace 头规范 | 总负责人 | ☐ |
| 9 | CORS 域名清单（方案 B/C） | 前端 | ☐ |
| 10 | 验收标准确认与排期 | 总负责人 | ☐ |
