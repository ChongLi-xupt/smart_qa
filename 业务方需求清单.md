# 智慧问数系统（NL2SQL）业务方需求清单

> 交付接口：`POST /api/v1/ask`（同步问答）、`POST /api/v1/ask_stream`（SSE 流式问答），
> 附带前端页面 `GET /`、会话管理 `/api/v1/sessions*`、健康检查 `GET /healthz`。
> 本文档列出将该接口部署上线所需**业务方/运维方提供**的全部资源与信息。

---

## 一、技术栈概览（供评估资源）

| 组件 | 说明 |
|---|---|
| 运行时 | Python **≥ 3.10** |
| Web 框架 | FastAPI + uvicorn（生产建议 `--workers 1~2`） |
| 数据库 | MySQL（**只读访问**，PyMySQL 驱动） |
| 大模型 | OpenAI 兼容接口（默认 DeepSeek，可换任意兼容网关） |
| 会话存储 | 进程内存（默认）/ Redis（多 worker 时必选，可选组件） |

---

## 二、业务方必须提供

### 1. MySQL 数据库访问

| 提供项 | 要求 | 示例 |
|---|---|---|
| 连接地址 | host + port | `192.168.10.114:3306` |
| 数据库名 | 需要问答的库 | `visionanalysis` |
| **只读账号** | 仅授 `SELECT` 权限，**禁止使用 root/写权限账号** | `qa_ro` / 密码 |
| 网络放行 | 部署服务器 IP → 数据库 3306 端口白名单/安全组 | — |
| 表结构说明 | 表/字段 `COMMENT` 备注尽量完整；备注缺失时提供字段含义对照表 | 见 §四 |

只读账号授权 SQL（业务方 DBA 执行）：

```sql
CREATE USER 'qa_ro'@'%' IDENTIFIED BY '<强密码>';
GRANT SELECT ON visionanalysis.* TO 'qa_ro'@'%';
FLUSH PRIVILEGES;
```

> 系统对数据库**只执行 SELECT**，并有 SQL 安全护栏（sqlglot AST 校验，拦截写/DDL/多语句），
> 但账号最小权限仍是必须的安全基线。

### 2. 大模型服务（LLM）

| 提供项 | 要求 | 示例 |
|---|---|---|
| 接口地址 | OpenAI 兼容的 Base URL | `https://api.deepseek.com/v1` 或企业内模型网关 |
| API Key | 具备足够配额/并发 | `sk-xxxx` |
| 模型名 | 支持函数调用（tool calling） | `deepseek-chat` |
| 网络放行 | 部署服务器 → 模型服务端口（公网 443 或内网端口） | — |

> 单次提问涉及多轮工具调用，典型耗时 **10~60 秒**，模型侧超时需 ≥ 60 秒；
> 如使用企业内网模型网关，请确认网关对部署服务器 IP 放行。

### 3. 服务器与网络

| 提供项 | 要求 |
|---|---|
| 应用服务器 | 1 台，Linux 优先；建议配置 ≥ 2C4G（瓶颈在 LLM 与 DB，不在本机） |
| 系统依赖 | Python ≥ 3.10、pip；可选 Redis 服务（见 §三） |
| 服务端口 | 默认 `5000`（可改），需在**服务器防火墙 + 云安全组**对访问方放行 |
| 访问范围确认 | 仅内网 / 局域网 / 公网？决定是否需要域名 + HTTPS |
| 域名 + HTTPS 证书 | **公网暴露时必提供**（启用 `SESSION_COOKIE_SECURE=true`） |

### 4. 访问控制策略（二选一）

| 方案 | 说明 |
|---|---|
| A. API Key（内置） | 业务方提供密钥列表（逗号分隔），配置到 `WEB_API_KEYS`；调用方请求头携带 `X-API-Key` |
| B. 统一网关/SSO | 业务方网关做认证后转发，本服务 `WEB_API_KEYS` 留空、仅对网关 IP 开放 |

---

## 三、业务方按需提供（影响体验与可用性）

| 提供项 | 作用 | 格式 |
|---|---|---|
| Redis 服务地址 | 多 worker/多实例部署时会话历史共享；单机单 worker 可不要 | `redis://host:6379/0` |
| 业务术语表 | 注入系统提示词，显著提升 NL2SQL 准确率 | `rag/glossary.md`，每行「术语: 定义」 |
| 高频问答语料 | few-shot 示例，提问时自动检索 Top-3 注入 | `rag/examples.jsonl`，每行 `{"question","sql"}` |
| 字段中文别名表 | 图表图例/表头展示中文（备注缺失时的兜底） | `config/column_aliases.yaml` |
| 敏感字段清单 | 系统内置密码/身份证/手机号/邮箱等拦截；业务方可补充行业敏感项 | 文本清单 |
| 进程托管与日志 | systemd/supervisor 托管、stdout 日志采集 | 运维侧配置 |
| 监控接入 | `GET /healthz` 返回数据库连通性与 LLM 配置状态，可挂监控告警 | — |

---

## 四、配置交接表（业务方填值后交开发/运维落地）

业务方只需填写下表「业务方填写」列，其余由部署方配置：

| 环境变量 | 业务方填写 | 必填 | 说明 |
|---|---|---|---|
| `DB_HOST` / `DB_PORT` | ✔ | 是 | MySQL 地址 |
| `DB_USER` / `DB_PASSWORD` | ✔ | 是 | **只读账号**凭据 |
| `DB_NAME` | ✔ | 是 | 目标库名 |
| `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL` | ✔ | 是 | 模型服务三要素 |
| `WEB_API_KEYS` | ✔（或走网关） | 建议 | 接口访问密钥，逗号分隔 |
| `REDIS_URL` | 可选 | 否 | 多 worker 时必填 |
| `WEB_HOST` / `WEB_PORT` | — | — | 部署方定，默认 `0.0.0.0:5000` |
| `SESSION_SECRET_KEY` | — | — | 部署方生成（≥32 位随机串） |
| `SESSION_COOKIE_SECURE` | — | — | HTTPS 部署时部署方置 `true` |
| `RATE_LIMIT_ASK` 等 | — | — | 限流阈值，默认 20 次/分钟/IP |

---

## 五、网络开通清单（防火墙/安全组）

| 方向 | 端口 | 用途 |
|---|---|---|
| 客户端 → 应用服务器 | 5000（或 443） | 页面与 API 访问 |
| 应用服务器 → MySQL | 3306 | 只读查询 |
| 应用服务器 → LLM 服务 | 443 / 网关端口 | 大模型调用 |
| 应用服务器 → Redis | 6379 | 会话共享（可选，同机可免） |

---

## 六、上线验收标准

1. `GET /healthz` 返回 `{"status":"ok","db":"ok","llm_configured":true}`；
2. `POST /api/v1/ask` 携带 `{"question":"...","user_id":"..."}` 返回 200 结构化回答（含 SQL/数据/图表建议）；
3. 敏感问题（如「查询用户密码」）被拦截且**不执行 SQL、不写历史**；
4. 不同 `user_id` 的会话列表互相隔离（`GET /api/v1/sessions?user_id=xxx`）；
5. 未携带/错误 `X-API-Key` 时（配置密钥后）返回 401。

---

## 七、联系与交付物

- 业务方交付：数据库只读账号、LLM 凭据、网络白名单回执、术语表/语料（可选）、访问控制方案确认。
- 开发方交付：部署完成的服务地址、`API.md` 接口文档、本验收报告。
